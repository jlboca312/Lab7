<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- "Mark Favorite", should appear as action button if possible -->
    <item
        android:id="@+id/addButton"
        android:icon="@drawable/ic_add_black_48dp"
        android:title="@string/add_button"
        app:showAsAction="ifRoom"/>


    <item android:id="@+id/leftButton"
        android:icon="@drawable/ic_keyboard_arrow_left_black_48dp"
        android:title="@string/left_arrow"
        app:showAsAction="ifRoom"/>

    <item android:id="@+id/rightButton"
        android:icon="@drawable/ic_keyboard_arrow_right_black_48dp"
        android:title="@string/right_arrow"
        app:showAsAction="ifRoom"/>



</menu>